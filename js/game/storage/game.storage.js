
window.game = window.game || {};

window.game.storage = function () {
    var _storage = {};
    
    // save object 
    _storage.save = function (path, object) {

    };

    // load object
    _storage.load = function (path) {

    };

    return _storage;
};